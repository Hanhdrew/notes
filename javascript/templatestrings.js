//template strings are string literals hard coded template literal whatever

const name = "Andrew";
const skyScraper = true;

console.log(`How are you doing ${name}`);

//shorthand if else statement - you can also add logic inside interpolation

console.log(
  `${name} is the ${skyScraper ? "tallest" : "shortest"} \nperson i know`, // \n to create a line break or in template literal just enter to get a line break
);
